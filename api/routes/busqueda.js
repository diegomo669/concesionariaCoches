// Ejemplo usando Express.js
/*app.get('/coches/:modelo', (req, res) => {
    const modelo = req.params.modelo;
    const coche = coches.find(c => c.modelo === modelo); // 'coches' es tu array de datos
    if (coche) {
      res.json({ success: true, coche });
    } else {
      res.json({ success: false });
    }
  });
  */